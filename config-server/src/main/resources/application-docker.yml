server:
  port: 8084

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/guilhermeozana/jobboard-app-config

#Eureka
eureka:
  client:
    serviceUrl:
      defaultZone: http://servicereg:8761/eureka

