server:
  port: 8084

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/guilhermeozana/jobboard-backend/tree/main/config-server/repo
          search-paths: '{application}'

#Eureka
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

